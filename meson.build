project('island-lab', 'c',
        version : 'PRE-1.0')

sys_dep = meson.get_compiler('c').find_library('argp',
                                               required : false)

subdir('src')

# vi:ts=2:sw=2:et
